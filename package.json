{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter @workspace/api-server --filter @workspace/jamuhuri-website run dev",
    "build": "pnpm -r run build",
    "typecheck": "tsc --build --emitDeclarationOnly"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "pnpm": {
    "overrides": {
      "lodash": "4.18.0",
      "path-to-regexp": "8.4.0",
      "picomatch@2": "2.3.2",
      "picomatch@4": "4.0.4"
    }
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.104.0",
    "@types/node": "catalog:",
    "dotenv": "^17.4.2"
  }
}
